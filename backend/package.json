{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --no-notify --respawn --transpileOnly src/index",
    "update-db": "prisma migrate save --experimental && prisma migrate up --experimental",
    "generate": "prisma generate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.0.14",
    "graphql-tools": "^6.0.11",
    "ts-node-dev": "^1.0.0-pre.49",
    "typescript": "^3.9.5"
  },
  "dependencies": {
    "@prisma/cli": "^2.1.3",
    "@prisma/client": "^2.1.3",
    "apollo-server": "^2.15.0",
    "graphql": "^15.2.0"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
