type Query {
    messages(limit: Int = 25): [Message!]!
}

type Mutation {
    message(input: MessageInput!): Message!
}

type Message {
    id: ID!
    content: String!
    createdAt: String!
}

input MessageInput {
    content: String!
}
